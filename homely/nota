.menu {
  width: 65%;
  display: flex;
  flex-direction: column;
  position: absolute;
  right: 0;
  margin-bottom: -350px;
  /* margin-right: -30px; */
  padding: var(--space-m);
}

.is-active {
  width: 65%;
  display: flex;
  flex-direction: column;
  position: absolute;
  right: 0;
  margin-bottom: -500px;
  margin-top: -150px; 
  background-color: var(--bg-color);
}